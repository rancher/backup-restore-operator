# Configuration for release/v10.x branch
branch: release/v10.x

# K3S versions for integration tests
k3s_versions:
  - v1.33.8-k3s1
  - v1.35.1-k3s1

# Automation-core reference (branch/tag/sha)
automation_core_ref: v1  # Pin to major version (stable with updates)

# Workflow enablement
workflows:
  ci: true
  release: true
  head-builds: true
  fossa: true

# Metadata
description: "Release branch for BRO v10.x"
