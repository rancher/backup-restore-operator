name: build-deps
description: Install the tooling needed to run the CI build job

runs:
  using: composite
  steps:
    - name: Checkout bin-deps config to local branch
      shell: bash
      run: git restore --source automation-core .bin-deps.yaml

    - uses: rancherlabs/dep-fetch/actions/sync-deps@c20b0b0e73e0819fd96fab41e1ee8987714dd04b # v0.3.0

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4.1.0

    - name: Install Helm-unittest
      shell: bash
      run: |
        ci-select helm helmv4 && \
        helm plugin install https://github.com/helm-unittest/helm-unittest.git --verify=false

    - name: Install python
      shell: bash
      run: |
        zypper -n install --no-recommends \
            python311 \
            python311-pip \
            && zypper clean -a \
            && ln -sf /usr/bin/python3.11 /var/ci-tools/active/python3

    - name: Set up chart-testing
      uses: helm/chart-testing-action@0d28d3144d3a25ea2cc349d6e59901c4ff469b3b # v2.7.0
