#!/bin/bash
set -e

cd $(dirname $0)/..

echo "Tidying workspace modules"

for module in . tests; do
    echo "go mod tidy: $module"
    (cd "$module" && go mod tidy)
done

echo "go work sync"
go work sync
